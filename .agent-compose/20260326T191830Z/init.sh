#!/bin/bash
# No build system or dependencies to install — vanilla HTML/CSS/JS project
# Served as static files via Dockerfile (nginx) or direct file access
echo "No setup required for this vanilla HTML/CSS/JS project."
